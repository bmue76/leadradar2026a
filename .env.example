# LeadRadar2026A – Example env (NO secrets)

# Local Dev DB (known credentials on port 5433)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/leadradar2026a?schema=public"

# TP 2.5 — Mobile API Key hashing (HMAC-SHA256)
# IMPORTANT: set this in .env.local / production envs (NOT in git).
# Recommend >= 32 random bytes (base64url or hex).
MOBILE_API_KEY_SECRET=""
